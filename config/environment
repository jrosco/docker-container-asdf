# this repos build number/tag
BUILD_NUMBER="0"

# asdf version
ASDF_MAJOR_VERSION="0"
ASDF_MINOR_VERSION="16"
ASDF_PATCH_VERSION="1"
ASDF_VERSION_SHORTHAND="$ASDF_MAJOR_VERSION.$ASDF_MINOR_VERSION"
ASDF_VERSION="v$ASDF_MAJOR_VERSION.$ASDF_MINOR_VERSION.$ASDF_PATCH_VERSION"
# golang - https://hub.docker.com/_/golang
GOLANG_VERSION="1.23.6"

# labels
NAME="asdf-vm"
MAINTAINER="jrosco"
DESCRIPTION="asdf version manager toolset"
URL="https://github.com/jrosco/docker-container-asdf"
REPOSITORY=$URL

# docker registries
ASDF_REGISTRY_PREFIX=""
ASDF_REGISTRY_SUFFIX=""
ASDF_REGISTRY_BASE="ghcr.io/jrosco/${ASDF_REGISTRY_PREFIX}${NAME}${ASDF_REGISTRY_SUFFIX}"
ASDF_REGISTRY_TOOLSET="$ASDF_REGISTRY_BASE"
TAG_PREFIX=""
TAG_SUFFIX=""

# alpine - https://hub.docker.com/_/alpine/
ALPINE_IMAGE_REPO_PATH="${ASDF_REGISTRY_BASE}:${TAG_PREFIX}alpine${TAG_SUFFIX}"
ALPINE_TAG_PREFIX=""
ALPINE_TAG_SUFFIX=""
ALPINE_MAJOR_VERSION="3"
ALPINE_MINOR_VERSION="20"
ALPINE_PATCH_VERSION="6"
ALPINE_VERSION="$ALPINE_MAJOR_VERSION.$ALPINE_MINOR_VERSION.$ALPINE_PATCH_VERSION"
ALPINE_VERSION_SHORTHAND="$ALPINE_MAJOR_VERSION.$ALPINE_MINOR_VERSION"

# debian - https://hub.docker.com/_/debian
DEBIAN_IMAGE_REPO_PATH="$ASDF_REGISTRY_BASE:${TAG_PREFIX}debian${TAG_SUFFIX}"
DEBIAN_TAG_PREFIX=""
DEBIAN_TAG_SUFFIX=""
DEBIAN_MAJOR_VERSION="12"
DEBIAN_MINOR_VERSION="8"
DEBIAN_VERSION="$DEBIAN_MAJOR_VERSION.$DEBIAN_MINOR_VERSION"
DEBIAN_VERSION_CODENAME="bookworm"

# disabled redhat as it does not support golang 1.24.4 required for asdf 
# when ready to enable see
# https://catalog.redhat.com/software/containers/ubi8/go-toolset/5ce8713aac3db925c03774d1?gs&q=go%20tool 
# redhat
# REDHAT_IMAGE_REPO_PATH="$ASDF_REGISTRY_BASE:redhat"
# REDHAT_TAG_PREFIX=""
# REDHAT_TAG_SUFFIX=""
# REDHAT_VERSION="8.9"
# REDHAT_MAJOR_VERSION="8"
# REDHAT_MINOR_VERSION="9"
# REDHAT_VERSION_SHORTHAND="ubi8"

# add your asdf packages
AWSCLI_PACKAGES="awscli:latest:set"
HELM_PACKAGES="helm:latest:set helmfile:latest:set helm-diff:latest:set"
TERRAFORM_PACKAGES="terraform:latest:set tflint:latest:set terraform-validator:latest:set"
DEVOPS_PACKAGES="$AWSCLI_PACKAGES $HELM_PACKAGES $TERRAFORM_PACKAGES"
